name: Run Python
on: 
    push:

jobs:
    bulid:
        name: run py1
        runs on: ubunto-latest
        steps:
        - uses: actions/checkout@v2
        - name: Run python
            run:
              python3 test.py